{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "codelldb",
            "request": "launch",
            "name": "Debug assembler",
            "program": "${workspaceFolder}/target/debug/mos6502",
            "args": [
                "asm",
                "examples/loop.asm"
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}
